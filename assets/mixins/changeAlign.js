export default {
    methods: {
        changeAlign(language) {
            if (language === "fa") {
                return "right";
            } else {
                return "left";
            }
        },
    }
}